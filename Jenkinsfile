pipeline {
    agent any

    stages {
        stage("Buid Stage") {
            steps {
                echo "========executing A========"
                echo "Hello, World!"
            }
        }
    }
}
