pipeline {
    agent any

    stages {
        stage("A") {
            steps {
                echo "========executing A========"
                echo "Hello, World!"
            }
        }
    }
}
